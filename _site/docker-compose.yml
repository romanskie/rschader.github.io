jekyll:
        image: jekyll/jekyll:pages
        command: jekyll serve --watch --incremental
        ports:
                - 4000:4000
        volumes:
                - /home/romanskie/dev/romanskie.github.io:/srv/jekyll
